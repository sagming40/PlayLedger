# PlayLedger — 화면 설계서

> 모바일 앱 화면 구성 및 UI 동작 규격 정의

- **작성일**: 2026-08-13
- **작성자**: 사공민규
- **버전**: v0.1 (초안)
- **관련 문서**: 요구사항 정의서(01), 시스템 아키텍처(02)

---

## 1. 화면 목록

| ID | 화면명 | 우선순위 | 마일스톤 | 설명 |
|---|---|:---:|:---:|---|
| S-01 | 로그인 | P0 | M2 | 로그인 / 회원가입 전환 |
| S-02 | 라이브러리 | P0 | M2 | 보유 게임 목록 (앱의 홈) |
| S-03 | 게임 등록·수정 | P0 | M2 | 게임 정보 입력 폼 |
| S-04 | 게임 상세 | P0 | M3 | 단일 게임 정보 + 상태 변경 |
| S-05 | 통계 | P1 | M4 | 백로그 대시보드 |
| S-06 | 설정 | P1 | M5 | Steam 연동, 로그아웃 |

---

## 2. 네비게이션 구조

```
[앱 시작]
    │
    ├─ 토큰 없음 ──▶ S-01 로그인
    │                    │ 로그인 성공
    │                    ↓
    └─ 토큰 있음 ──▶ ┌──────────────────────┐
                    │   하단 탭 네비게이션      │
                    ├──────────────────────┤
                    │ [라이브러리] [통계] [설정] │
                    └──────────────────────┘
                         │        │       │
                       S-02     S-05    S-06
                         │
                    ┌────┴────┐
                    ↓         ↓
                  S-04      S-03
                (상세)     (등록)
```

**하단 탭을 쓰는 이유**

주요 화면이 3개이고 서로 대등한 관계다. 서랍(drawer) 메뉴는 한 번 더
눌러야 하고, 상단 탭은 한 손 조작에 불리하다. 모바일에서 가장 손이
닿기 쉬운 위치는 화면 아래쪽이다.

**S-03, S-04를 탭에 넣지 않는 이유**

이 둘은 "특정 게임"에 종속된 화면이라 대등한 관계가 아니다.
라이브러리에서 진입해 뒤로가기로 돌아오는 스택 구조가 맞다.

---

## 3. 화면별 상세

### 3.1 S-01 로그인

```
┌──────────────────────────┐
│                          │
│                          │
│       PlayLedger         │
│   사놓고 안 한 게임, 관리    │
│                          │
│  ┌────────────────────┐  │
│  │ 아이디               │  │
│  └────────────────────┘  │
│  ┌────────────────────┐  │
│  │ 비밀번호             │  │
│  └────────────────────┘  │
│                          │
│  ┌────────────────────┐  │
│  │       로그인         │  │
│  └────────────────────┘  │
│                          │
│    계정이 없으신가요? 가입    │
│                          │
└──────────────────────────┘
```

| 항목 | 규격 |
|---|---|
| 입력 검증 | 빈 값이면 버튼 비활성. 서버 응답 전 중복 제출 차단 |
| 실패 표시 | 입력 필드 아래 빨간 텍스트. 알림창 사용 안 함 |
| 토큰 보관 | 로그인 성공 시 기기에 저장. 앱 재시작 시 자동 로그인 |
| 가입 전환 | 같은 화면에서 폼만 전환 (이메일 필드 추가) |

> **실패 메시지 원칙:** "아이디가 없습니다" / "비밀번호가 틀렸습니다"로
> 나누지 않고 "아이디 또는 비밀번호가 올바르지 않습니다"로 통일한다.
> 나누면 어떤 아이디가 존재하는지 알려주는 셈이 된다.

---

### 3.2 S-02 라이브러리 (홈)

```
┌──────────────────────────┐
│ 라이브러리          [ + ]  │  ← 헤더
├──────────────────────────┤
│ [전체] [미시작] [플레이중]   │  ← 상태 필터 (가로 스크롤)
│ [클리어] [보류] [포기]      │
├──────────────────────────┤
│ 총 42개 · 미시작 18개       │  ← 요약 줄
├──────────────────────────┤
│ ┌──────────────────────┐ │
│ │ 사이버펑크 2077        │ │
│ │ ● 클리어  ·  62.4시간   │ │
│ │ RPG, 오픈월드          │ │
│ └──────────────────────┘ │
│ ┌──────────────────────┐ │
│ │ 발더스 게이트 3         │ │
│ │ ● 미시작  ·  방치 214일  │ │
│ │ RPG                   │ │
│ └──────────────────────┘ │
│           ⋮               │
└──────────────────────────┘
     [라이브러리] [통계] [설정]
```

| 항목 | 규격 |
|---|---|
| 정렬 기본값 | 최근 수정순 |
| 정렬 옵션 | 최근 수정 / 구매일 / 제목 / 플레이타임 |
| 목록 로딩 | 20개씩. 끝에 도달하면 추가 로드 |
| 카드 탭 | S-04 상세로 이동 |
| `+` 버튼 | S-03 등록으로 이동 |

**카드 두 번째 줄이 상태에 따라 바뀐다**

| 상태 | 표시 내용 |
|---|---|
| `BACKLOG` | `● 미시작 · 방치 214일` |
| `PLAYING` | `● 플레이중 · 12.5시간` |
| `CLEARED` | `● 클리어 · 62.4시간` |
| `ON_HOLD` | `● 보류 · 8.0시간` |
| `DROPPED` | `● 포기 · 1.2시간` |

미시작 게임에 플레이타임(0시간)을 보여주는 것은 정보가 아니다.
그 자리에 방치일수를 넣으면 이 앱이 풀려는 문제가 목록에서 바로 보인다.

---

### 3.3 S-03 게임 등록·수정

```
┌──────────────────────────┐
│ ← 게임 등록         [저장] │
├──────────────────────────┤
│ 제목 *                    │
│ ┌──────────────────────┐ │
│ │                      │ │
│ └──────────────────────┘ │
│                          │
│ 상태 *                    │
│ [미시작▾]                 │
│                          │
│ 장르                      │
│ [+ RPG] [+ 액션] [+ ...]  │  ← 다중 선택 칩
│                          │
│ 구매일                    │
│ [2026-01-15]             │
│                          │
│ 구매가 (원)                │
│ ┌──────────────────────┐ │
│ └──────────────────────┘ │
│                          │
│ 플레이타임 (시간)           │
│ ┌──────────────────────┐ │
│ └──────────────────────┘ │
│                          │
│ ── 클리어/포기 시에만 표시 ── │
│ 평점  ☆☆☆☆☆              │
│ 한줄평                    │
│ ┌──────────────────────┐ │
│ └──────────────────────┘ │
└──────────────────────────┘
```

| 항목 | 규격 |
|---|---|
| 필수 항목 | 제목, 상태. 나머지는 선택 |
| 조건부 표시 | 평점·한줄평은 상태가 `CLEARED` 또는 `DROPPED`일 때만 노출 |
| 중복 경고 | 저장 시 서버가 중복으로 판정하면 "이미 등록된 게임입니다" 표시 |
| 수정 모드 | 같은 화면 재사용. 기존 값 채워서 진입 |

**평점을 조건부로 숨기는 이유**

아직 시작도 안 한 게임에 평점 칸이 보이면 뭘 적어야 하나 고민하게 된다.
입력할 수 없는 항목은 보이지 않는 편이 낫다.

---

### 3.4 S-04 게임 상세

```
┌──────────────────────────┐
│ ←              [수정][⋮] │
├──────────────────────────┤
│ 사이버펑크 2077            │
│ RPG · 오픈월드             │
│                          │
│ ┌──────────────────────┐ │
│ │ 상태   [클리어 ▾]      │ │  ← 여기서 바로 변경
│ └──────────────────────┘ │
│                          │
│ 구매일        2026-01-15  │
│ 구매가          66,000원  │
│ 플레이타임        62.4시간 │
│ ─────────────────────── │
│ 시간당 비용     1,058원/h  │  ← 서버 계산값
│ ─────────────────────── │
│ 평점            ★★★★☆    │
│ 한줄평                    │
│ "엔딩 보고 한참 멍했음"      │
│                          │
│ 출처              직접 입력 │
└──────────────────────────┘
```

| 항목 | 규격 |
|---|---|
| 상태 변경 | 상세 화면에서 바로 변경 (수정 화면 안 거침) |
| 시간당 비용 | 플레이타임 0이면 `—` 표시 |
| `⋮` 메뉴 | 삭제 (확인 대화상자 필수) |
| 출처 표시 | `직접 입력` / `Steam 연동` |

**상태만 별도로 변경할 수 있게 하는 이유**

가장 자주 하는 조작이 "미시작 → 플레이중" 전환이다.
이걸 수정 화면 진입 → 항목 찾기 → 저장의 3단계로 만들면 안 쓰게 된다.

---

### 3.5 S-05 통계

```
┌──────────────────────────┐
│ 통계                      │
├──────────────────────────┤
│ ┌────────┐ ┌────────┐   │
│ │  42    │ │  18    │   │
│ │ 총 보유  │ │ 미시작  │   │
│ └────────┘ └────────┘   │
│ ┌────────┐ ┌────────┐   │
│ │ 31.0%  │ │ 42.9%  │   │
│ │ 사장률   │ │ 완주율  │   │
│ └────────┘ └────────┘   │
├──────────────────────────┤
│ 오래 방치된 게임             │
│ 1. 발더스 게이트 3   214일   │
│ 2. 엘든 링          180일   │
│ 3. 데스 스트랜딩     156일   │
│                 [더보기]   │
├──────────────────────────┤
│ 시간당 비용이 높은 게임        │
│ 1. 게임 A      33,000원/h  │
│ 2. 게임 B      12,400원/h  │
├──────────────────────────┤
│ 장르별 완주율                │
│ RPG      ▓▓▓▓▓░░░░  52%  │
│ 액션      ▓▓▓░░░░░░  31%  │
│ 전략      ▓░░░░░░░░  12%  │
└──────────────────────────┘
```

| 항목 | 규격 |
|---|---|
| 계산 주체 | 전부 서버. 앱은 받은 값을 표시만 |
| 갱신 시점 | 화면 진입 시마다 조회 |
| 목록 항목 수 | 기본 3개, 더보기로 확장 |

---

### 3.6 S-06 설정

```
┌──────────────────────────┐
│ 설정                      │
├──────────────────────────┤
│ 계정                      │
│   사공민규                 │
│   sagming40@example.com  │
├──────────────────────────┤
│ Steam 연동                │
│   Steam ID               │
│   ┌────────────────────┐ │
│   │ 7656119...         │ │
│   └────────────────────┘ │
│   ┌────────────────────┐ │
│   │   지금 동기화         │ │
│   └────────────────────┘ │
│   마지막 동기화: 2시간 전    │
├──────────────────────────┤
│   로그아웃                 │
└──────────────────────────┘
```

**동기화 결과 표시**

동기화 후 "추가 12개 · 갱신 30개 · 변경 없음 5개"처럼 요약을 보여준다.
"완료되었습니다"만 뜨면 뭐가 바뀌었는지 알 수 없어 불안하다.

---

## 4. 상태 표시 규칙

### 4.1 진행 상태 색상

| 상태 | 색상 계열 | 비고 |
|---|---|---|
| `BACKLOG` | 회색 | 아직 시작 안 함 |
| `PLAYING` | 파랑 | 진행 중 |
| `CLEARED` | 초록 | 완료 |
| `ON_HOLD` | 노랑 | 멈춤 |
| `DROPPED` | 흐린 회색 | 종료 |

**색만으로 구분하지 않는다.** 모든 상태 표시는 색과 텍스트를 함께 쓴다.
색각 이상이 있는 사용자도 구분할 수 있어야 한다.

### 4.2 방치일수 강조

| 구간 | 표시 |
|---|---|
| 30일 미만 | 기본 |
| 30 ~ 179일 | 강조 없음, 일수만 표시 |
| 180일 이상 | 주의 색상 |

---

## 5. 빈 상태 (Empty State)

신규 가입자는 데이터가 하나도 없다. 이 상태를 방치하면
"앱이 고장 났나" 싶은 화면이 된다.

| 화면 | 빈 상태 처리 |
|---|---|
| S-02 라이브러리 | "아직 등록된 게임이 없어요" + `게임 추가하기` 버튼 |
| S-02 필터 결과 없음 | "해당 상태의 게임이 없습니다" (버튼 없음) |
| S-05 통계 | "게임을 등록하면 통계가 표시됩니다" |

전체가 비었을 때와 필터 결과만 비었을 때는 다르게 처리한다.
전자는 행동을 유도하고, 후자는 필터를 바꾸면 되므로 안내만 한다.

---

## 6. 로딩 및 오류 처리

| 상황 | 처리 |
|---|---|
| 최초 로딩 | 목록 자리에 뼈대(skeleton) 표시 |
| 추가 로딩 | 목록 하단에 작은 표시자 |
| 저장 중 | 버튼 비활성 + 표시자. 중복 제출 차단 |
| 네트워크 오류 | "연결에 실패했습니다" + `다시 시도` 버튼 |
| 인증 만료(401) | 토큰 삭제 후 S-01로 이동 |

**로딩 중 화면 전체를 가리지 않는다.** 목록이 이미 있는데 새로고침한다고
빈 화면이 되면 사용자는 데이터가 사라진 줄 안다.

---

## 7. 범위 외 (1차 기준)

- 다크 모드 (시스템 설정 자동 감지로 대체)
- 게임 이미지 / 커버 아트 표시
- 검색 기능 (필터로 우선 대응)
- 애니메이션 및 전환 효과
- 태블릿 레이아웃

---

## 8. 변경 이력

| 버전 | 날짜 | 내용 |
|---|---|---|
| v0.1 | 2026-08-13 | 최초 작성 |
