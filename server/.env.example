# FastAPI 서버용 설정 ─ server/.env 예시 파일
# 비밀번호 root .env의 POSTGRES_PASSWORD와 반드시 같아야 한다
# JWT_SECRET_KEY= → python -c "import secrets; print(secrets.token_urlsafe(32))"로 랜덤값 생성
DATABASE_URL=
JWT_SECRET_KEY=
